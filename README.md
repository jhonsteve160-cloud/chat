# Simple Login + Chat App

### Default Login
- Username: **admin**
- Password: **12345**

### Run locally

```
npm install
npm start
```

### Railway Deployment
Just push to GitHub — Railway will auto-install and run `node server.js`.
